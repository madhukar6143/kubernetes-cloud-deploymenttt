- name: Deploy to OCI OKE
  run: |
    echo "${{ secrets.KUBECONFIG }}" > kubeconfig.yaml
    export KUBECONFIG=$(pwd)/kubeconfig.yaml

    # Test connection
    kubectl get nodes

    # Apply all YAML files safely
    kubectl apply -f k8s/ --validate=false
